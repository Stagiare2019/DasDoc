twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes: ['bootstrap_4_horizontal_layout.html.twig']
    globals:
        pdf_relative_directory: '%pdf_relative_directory%'
        # Emplacement des dossiers pour l'import
        import_csv_relative_directory: '%import_csv_relative_directory%'
        import_pdf_relative_directory: '%import_pdf_relative_directory%'